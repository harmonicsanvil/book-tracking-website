<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Login Page</title>
        <link rel="stylesheet" href="css/login.css">
    </head>
    <body>
        <div class="form">
            <form action = "login" method='POST'>
                <% if (isValid === -1){     %> 
                <small class="error"> Wrong username or password  </small>
                <% }   %>
                <% if (isValid === 0){     %> 
                    <small class="error"> Email not verified  </small>
                    <% }   %>
                <label for="username">Username</label>
                <input type='text' placeholder="Enter your username" class="input" id="username" name="username" required>
                <label for="password">Password</label>
                <input type='password' placeholder='Enter your password' class="input" id="password" name="password" required>
                <div class='button'>
                    <button type='submit' id="login"> Login </button>
                </div>
                <p> Not a user? <a href='register' id='register'> Register </a> </p>

            </form>
        </div>
    </body>
</html>